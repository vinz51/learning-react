import PropTypes from 'prop-types';
import React from 'react';

import Cell from './cell';

export default class TicTacToe extends React.Component {
	constructor() {
		// super(props) doesn't cut it anymore
		// because 2nd arg is context
		super(...arguments);
	}
	render() {

	}
}
